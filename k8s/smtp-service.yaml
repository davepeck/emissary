apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: smtp
  name: smtp
spec:
  ports:
    - name: "8025"
      port: 8025
      targetPort: 8025
  selector:
    io.kompose.service: smtp
